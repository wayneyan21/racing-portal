
# Racing Portal (MySQL)
Quick start:
1) `cp .env.example .env` (fill DB_PASS if needed)
2) `npm install`
3) `mysql -u root -p < setup.sql`  # create schema + demo rows
4) `npm start`
Open http://localhost:3000  (login: admin / Wayne123!)
